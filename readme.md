# SG Module
This module is developed for joindevops company.Project inside the joindevops company will follows this module to create their security group

## Inputs
* project (required/mandatory) -> users should specify their project name
* environment (required/mandatory) -> Users should specify thier environment
* sg_name (required/mandatory) -> Users should specify thier environment
* sg_discription (required/mandatory) -> Users should specify thier environment
* vpc_id (required/mandatory) -> Users should specify thier environment

##  Outputs
* sg_id  Exportes, sg_id created...